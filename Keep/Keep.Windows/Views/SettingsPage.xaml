<Page
    x:Class="Keep.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Keep"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    Background="{ThemeResource AppStatusBarBackgroundBrush}"
    DataContext="{Binding Source={StaticResource SettingsViewModel}}"
    d:DataContext="{d:DesignData /SampleData/SettingsViewModelSampleData.xaml}">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition/>
        </TransitionCollection>
    </Page.Transitions>
    
    <Page.BottomAppBar>
        <CommandBar x:Name="CommandBar" IsOpen="True" IsSticky="True">
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Uid="PraiseAppBarButton" Icon="Like" Label="praise" Command="{Binding SendFeedbackCommand}"/>
                <AppBarButton x:Uid="SupportAppBarButton" Icon="Mail" Label="support" Command="{Binding SuggestFeatureOrReportBugCommand}"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel x:Name="ContentRoot">
        </StackPanel>
    </Grid>
</Page>