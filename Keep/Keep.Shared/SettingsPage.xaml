<Page
    x:Class="Keep.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Keep"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignData /SampleData/SettingsViewModelSampleData.xaml}">

    <StackPanel Margin="24">
        <TextBlock Text="Theme" Style="{StaticResource TitleTextBlockStyle}"/>
        <ComboBox x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged" SelectedValue="{Binding UserPreferences.Theme}">
            <ComboBoxItem Tag="light" Content="Light" />
            <ComboBoxItem Tag="dark" Content="Dark"/>
        </ComboBox>
    </StackPanel>
</Page>
