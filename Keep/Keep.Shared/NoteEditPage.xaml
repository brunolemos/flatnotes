<Page
    x:Class="Keep.NoteEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Keep"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Light"
    Background="{Binding Note.Color.Color}" 
    d:DataContext="{d:DesignData /SampleData/NoteEditViewModelSampleData.xaml}">
    
    <StackPanel>
		<Grid Height="200" Visibility="Collapsed">
            <Grid.Background>
                <SolidColorBrush Color="{StaticResource MediaControlPanelVideoThemeColor}"/>
            </Grid.Background>
        </Grid>
        <TextBox PlaceholderText="Title" Text="{Binding Note.Title, Mode=TwoWay}" InputScope="Default" Background="{x:Null}" BorderThickness="0" FontWeight="Bold" Padding="24,12" Margin="0,12,0,0" Foreground="{StaticResource KeepNoteForegroundBrush}" TextWrapping="Wrap"/>
        <TextBox PlaceholderText="Text" Text="{Binding Note.Text, Mode=TwoWay}" InputScope="Default" Background="{x:Null}" BorderThickness="0" Margin="0" Padding="24,6" AcceptsReturn="True" Foreground="{StaticResource KeepNoteForegroundBrush}" TextWrapping="Wrap"/>
	</StackPanel>
</Page>
