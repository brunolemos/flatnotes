<Page
    x:Class="FlatNotes.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlatNotes.Controls"
    xmlns:utils="using:FlatNotes.Utils"
    xmlns:models="using:FlatNotes.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{x:Bind Path=viewModel}"
    d:DataContext="{d:DesignData /SampleData/MainViewModelSampleData.xaml}">

    <Page.Resources>
        <DataTemplate x:Key="NavLinkItemTemplate" x:DataType="utils:NavLink">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Width="68" Symbol="{x:Bind Symbol}" VerticalAlignment="Center"/>
                <TextBlock Text="{x:Bind Label}" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    
    <Page.TopAppBar>
        <CommandBar Background="{StaticResource AppMainDarkenBrush}" Foreground="White" RequestedTheme="Dark">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal">
                    <AppBarButton x:Name="MenuToggleButton" MinWidth="1" Width="Auto" IsCompact="True" Margin="0,0,12,0" Padding="0" VerticalAlignment="Stretch" Click="MenuToggleButton_Click" ToolTipService.ToolTip="Toggle menu">
                        <FontIcon Glyph="&#xE700;" Margin="16,0" VerticalAlignment="Center"/>
                    </AppBarButton>

                     <TextBlock Text="Notes" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </CommandBar.Content>

            <CommandBar.PrimaryCommands>
                <!--<AppBarButton x:Uid="NewAppBarButton" Icon="Add" Label="new" Command="{Binding CreateTextNoteCommand}" ToolTipService.ToolTip="New note"/>-->
                <AppBarButton x:Uid="SearchAppBarButton" Icon="Find" Label="search" ToolTipService.ToolTip="Search notes"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.TopAppBar>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Uid="TextAppBarButton" Label="text" Command="{Binding CreateTextNoteCommand}" Icon="Add" ToolTipService.ToolTip="New text note"/>
                <AppBarButton x:Uid="ChecklistAppBarButton" Label="checklist" Command="{Binding CreateChecklistNoteCommand}" Icon="List" ToolTipService.ToolTip="New checklist note"/>
                <AppBarButton x:Uid="PhotoAppBarButton" Icon="Camera" Label="photo" Command="{Binding OpenImagePickerCommand}" ToolTipService.ToolTip="New note from image or camera"/>
                <AppBarButton x:Uid="AudioAppBarButton" Icon="Microphone" Label="audio" ToolTipService.ToolTip="New audio note"/>
            </CommandBar.PrimaryCommands>

            <!--<CommandBar.SecondaryCommands>
                <AppBarButton x:Uid="ArchivedNotesAppBarButton" Icon="Preview" Label="archived notes" Command="{Binding OpenArchivedNotesCommand}" ToolTipService.ToolTip="Open archived notes page"/>
                <AppBarButton x:Uid="SettingsAppBarButton" Icon="Setting" Label="settings" Command="{Binding OpenSettingsCommand}" ToolTipService.ToolTip="Open settings notes page"/>
            </CommandBar.SecondaryCommands>-->
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesControl.ItemStretch" Value="True"/>
                        <Setter Target="NotesControl.ItemWidth" Value="150"/>
                        <!--<Setter Target="NotesControl.Padding" Value="0,6"/>-->
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesControl.ItemStretch" Value="False"/>
                        <Setter Target="NotesControl.ItemWidth" Value="220"/>
                        <!--<Setter Target="NotesControl.Padding" Value="12,24"/>-->
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesControl.ItemStretch" Value="False"/>
                        <Setter Target="NotesControl.ItemWidth" Value="260"/>
                        <!--<Setter Target="NotesControl.Padding" Value="24"/>-->
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesControl.ItemStretch" Value="False"/>
                        <Setter Target="NotesControl.ItemWidth" Value="260"/>
                        <!--<Setter Target="NotesControl.Padding" Value="24"/>-->
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1200" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesControl.ItemStretch" Value="False"/>
                        <Setter Target="NotesControl.ItemWidth" Value="260"/>
                        <!--<Setter Target="NotesControl.Padding" Value="64,24"/>-->
                    </VisualState.Setters>
                </VisualState>

            <VisualState>
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="1500" />
                </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesControl.ItemStretch" Value="False"/>
                        <Setter Target="NotesControl.ItemWidth" Value="300"/>
                        <!--<Setter Target="NotesControl.Padding" Value="64,24"/>-->
                    </VisualState.Setters>
            </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <SplitView x:Name="MainSplitView" Margin="0" Padding="0"
                   PaneBackground="{StaticResource SystemControlBackgroundAltHighBrush}" 
                   DisplayMode="Overlay" IsPaneOpen="False" CompactPaneLength="68" OpenPaneLength="260" Canvas.ZIndex="100">
            <SplitView.Pane>
                <ListView x:Name="NavLinksList" Margin="0,12,12,12" Padding="0" SelectionMode="Single" 
                    ItemTemplate="{StaticResource NavLinkItemTemplate}" FontWeight="Medium" VerticalAlignment="Stretch"
                    ItemClick="NavLinksList_ItemClick" IsItemClickEnabled="True"
                    ItemsSource="{x:Bind NavLinks}">

                    <ListView.ItemContainerTransitions>
                        <TransitionCollection/>
                    </ListView.ItemContainerTransitions>

                    <ListView.Footer>
                        <ListView x:Name="NavFooterLinksList" Grid.Row="1" Margin="0" Padding="0" SelectionMode="None" 
                            ItemTemplate="{StaticResource NavLinkItemTemplate}" VerticalAlignment="Stretch" 
                            ItemClick="NavLinksList_ItemClick" IsItemClickEnabled="True" ScrollViewer.VerticalScrollMode="Disabled"
                            ItemsSource="{x:Bind NavFooterLinks}">
                            <ListView.ItemContainerTransitions>
                                <TransitionCollection/>
                            </ListView.ItemContainerTransitions>
                        </ListView>
                    </ListView.Footer>
                </ListView>
            </SplitView.Pane>

            <SplitView.Content>
                <local:NotesControl x:Name="NotesControl" DataContext="{Binding Notes}" ItemClick="NotesListView_ItemClick"/>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
